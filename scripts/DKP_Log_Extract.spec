# -*- mode: python ; coding: utf-8 -*-
# Build: from dkp folder run:
#   pip install pyinstaller
#   pyinstaller scripts/DKP_Log_Extract.spec
# Output: dist/DKP_Log_Extract.exe (no console window)

a = Analysis(
    ['dkp_log_extract_gui.py'],
    pathex=[],
    binaries=[],
    datas=[],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='DKP_Log_Extract',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
