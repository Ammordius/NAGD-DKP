# Supabase credentials for CLI scripts (update_supabase_event_times.py, update_raid_loot_assignments_supabase.py, etc.)
# Copy to .env and fill in from Supabase Dashboard → Project Settings → API
# Do not commit .env (it should be in .gitignore).

SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Optional: for pg_dump / CI daily backup. Dashboard → Database → Connection string (URI, Session mode).
# SUPABASE_DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres
